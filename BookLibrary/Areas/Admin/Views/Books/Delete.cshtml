@model BookLibrary.ViewModels.Books.BookDeleteViewModel

@{
    ViewData["Title"] = "Delete Book";
}

<div class="d-flex justify-content-center align-items-center" style="min-height: 70vh;">

    <div class="glass-card p-4 text-center" style="max-width: 500px; width: 100%;">

        <h2 class="text-danger mb-3">
            📕 Delete Book
        </h2>

        <p class="text-black mb-4">
            Are you sure you want to delete this book?
        </p>

        <div class="mb-4">
            <h5 class="text-black fw-bold">
                @Model.Title
            </h5>

            <p class="text-muted">
                @Model.Author
            </p>

            <p class="text-muted small">
                This book cannot be deleted if it is currently rented.
            </p>
        </div>

        <form asp-controller="Books" asp-action="DeleteConfirmed" method="post">
            <input type="hidden" asp-for="Id" />

            <div class="d-flex justify-content-center gap-3 mt-3">

                <button type="submit" class="btn btn-danger px-4">
                    Delete
                </button>

                <a asp-action="Index" class="btn btn-secondary px-4">
                    Cancel
                </a>

            </div>
        </form>

    </div>

</div>